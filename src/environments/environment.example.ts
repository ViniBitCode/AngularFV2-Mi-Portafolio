// Plantilla del environment. Copiar este archivo como `environment.ts`
// (ignorado por git) y completar los valores reales.
export const environment = {
  production: false,
  web3forms: {
    // Access key pública de Web3Forms: mapea a la casilla de destino en su
    // servidor, así el correo real nunca aparece en el código del front.
    // Se obtiene en https://web3forms.com (llega por mail a la casilla).
    accessKey: 'PEGAR_ACCESS_KEY_DE_WEB3FORMS',
  },
};
